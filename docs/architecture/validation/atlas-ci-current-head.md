# Atlas CI Current-Head Validation

This branch validates the current Atlas identity, role, task-visibility, Owner/Manager data paths, membership registry, regenerated lockfile, and strict `npm ci` workflow.
