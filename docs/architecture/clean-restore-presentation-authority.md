# Clean/Restore Presentation Authority

`operation_class = clean_restore` owns the reset presentation family.

Task origin, physical location, or legacy task type may specialize reset content, but they do not select a second card system.

This boundary changes presentation authority only. It does not change task truth, scheduling, transition semantics, completion semantics, resource truth, or operational state.
